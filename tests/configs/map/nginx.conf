events {}
http {
    map $uri $new {
        default 0;
        ~^/news 1;
        ~^/blog 2;
    }
    map $http_host $backend {
        hostnames;
        default web;
        *.example.com app;
    }
    server {
        listen 80;
    }
}
